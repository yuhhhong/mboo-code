spring:
  application:
    name: mboo-code
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:${mboo.appDataDir:${user.home}/.mboo}/mboo_data.sqlite
  sql:
    init:
      mode: always
      schema-locations: classpath:db/sqlite/schema.sql
  servlet:
    multipart:
      max-file-size: 4MB
      max-request-size: 6MB
  lifecycle:
    timeout-per-shutdown-phase: 20s

server:
  tomcat:
    max-part-count: 300
  shutdown: graceful

mboo:
  version: ${MBOO_VERSION:0.0.1-SNAPSHOT}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

# springdoc-openapi项目配置
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /doc.html
  group-configs:
    - group: default
      paths-to-match: /**
      packages-to-scan: com.yu.mboocode
