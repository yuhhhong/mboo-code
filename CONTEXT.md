# Code Agent

本上下文描述 Code Agent 在工作区任务、工具授权、会话管理、上下文管理和网络访问中采用的统一语言。

## 权限

**权限模式（`permissionMode`）**：
会话级开关，决定工具授权是否需要用户确认。当前取值为默认权限和完全访问，存储在 `Sessions.metadataJson` 顶层。

**默认权限（`DEFAULT`）**：
工具调用按权限内核正常评估；出现 `NEED_ASK` 时展示授权卡片并等待用户决策。

**完全访问（`FULL_ACCESS`）**：
将 `NEED_ASK` 自动视为 `ALLOWED`，不展示授权卡片；权限硬错误、内置命令黑名单和系统硬拒绝目标仍然拒绝。

**权限类型（`ToolPermissionType`）**：
工具声明的基础权限种类，包括 `NONE`、`TOOL`、`READ`、`WRITE`、`COMMAND` 和 `NETWORK`。

**权限要求（`PermissionRequirement`）**：
一次工具调用产生的单个授权诉求，包含权限类型、授权范围和当前评估结果。

**权限链（`ToolPermissionChain`）**：
一次工具调用的有序权限要求集合，例如工作区外命令的 `WRITE -> COMMAND`，或私有网络抓取的 `TOOL -> NETWORK`。

**评估结果（`PermissionCheck`）**：
单个权限要求的判定，包括 `ALLOWED`、`NEED_ASK` 和 `ERROR`。

**授权卡片（`TOOL_APPROVAL_REQUIRED`）**：
前端展示的授权确认项，对应一个待处理的授权阶段。

**单次授权（`ALLOW_ONCE`）**：
只允许当前工具调用，不持久化到会话权限。

**会话授权（`ALLOW_SESSION`）**：
在当前会话持续有效并持久化到 `Sessions.metadataJson.permissions` 的授权。

**授权目录（`grantPath`）**：
路径型权限申请授权的规范化绝对目录，授权范围包含其子目录。

**命令指纹（`allowedCommands` 元素）**：
绑定原始命令、真实工作目录和 Shell 身份的版本化哈希，用于 `COMMAND` 会话授权。

**内置命令黑名单（`COMMAND_PERMISSION_DENIED`）**：
由权限内核固定禁止的危险命令规则，任何权限模式都不能放行。

**执行前复核（`verifyFinalPlan`）**：
工具真实执行前重新生成权限链并核对授权范围，防止授权后参数或目标发生变化。

**联网授权**：
用户允许 Agent 将搜索条件或网络请求发送到本机之外的服务；它独立于文件读取和命令执行授权。
_Avoid_: 读取授权

**私有网络来源授权（`allowedNetworkOrigins` 元素）**：
用户对某个明确私有网络来源授予的会话级访问权限；不包含云平台元数据端点等系统硬拒绝目标。
_Avoid_: 私有网络访问开关

## 工作区

**保存工作区（`Workspace`）**：
用户选择的自定义目录对应的持久化记录，按规范化后的真实绝对路径唯一识别，用于侧边栏工作区分组。

**工作区名称（`Workspace Display Name`）**：
保存工作区真实绝对路径的末级文件夹名，仅用于展示；允许重名，不参与唯一性判断。

**工作区路径比较键（`Workspace.pathKey`）**：
由真实绝对路径和当前平台路径语义生成的内部唯一键；Windows 忽略大小写，Unix-like 系统保留大小写。

**工作区可用状态（`WorkspaceResp.available`）**：
读取工作区列表时，根据保存路径是否仍为可访问目录计算的展示状态；不持久化，也不改变会话归属。

**默认工作区（`Default Workspace`）**：
未选择自定义路径时，由后端在应用数据目录下为单个会话创建的独立目录；不生成保存工作区记录，对应侧边栏“任务”区域。

**工作区归属（`Sessions.workspaceId`）**：
会话在侧边栏中的显式分组关系。字段为空时归入“任务”，非空时归入对应保存工作区；执行路径仍由 `Sessions.workspacePath` 保存。旧数据中无法迁移的异常路径会话暂归“任务”。

**待提交工作区（`pendingWorkspacePath`）**：
新会话发送首条消息前的路径选择，可以改选或清除；它本身不会创建会话，最终值决定创建后的工作区归属。

**工作区删除（`Workspace Deletion`）**：
删除保存工作区记录及其下属会话和应用内会话制品，不删除工作区对应的用户磁盘目录。

**会话工作区（`Sessions.workspacePath`）**：
会话绑定的绝对目录，创建后保持不变；默认允许读取，写入仍需授权。

## 会话

**会话元数据（`Sessions.metadataJson`）**：
保存权限、权限模式等会话扩展数据的 JSON 字符串，更新使用分段锁和 CAS。

**会话事件（`SessionEvent`）**：
会话内追加的事件记录，写入 JSONL，并用于前端实时推送与历史回放。

## 上下文

**模型上下文能力上限（`maxContextLimit`）**：
models.dev 为当前模型提供的上下文窗口物理上限，只读，也是用户可调范围的最大值。

**模型上下文偏好（`ModelContextPreference`）**：
用户按供应商实际模型 ID 保存的自定义上下文窗口上限。

**生效上下文上限（`effectiveContextLimit`）**：
存在有效模型上下文偏好时使用偏好，否则使用模型上下文能力上限；作为输入上下文、压缩和硬预算的统一应用上限，不限制主模型输出。

**工作区指令（`Workspace Instructions`）**：
与会话工作区绑定、在主模型处理该工作区任务时持续生效的项目级行为指导；第一版由工作区根目录的 `AGENTS.md` 承载。

**工作区指令快照（`Workspace Instruction Snapshot`）**：
单个执行 turn 开始时读取并确定、在该 turn 内保持不变的一版工作区指令。

**运行环境上下文（`Runtime Environment Context`）**：
由运行时确定并提供给主模型的当前工作区、操作系统、Shell、日期和时区事实集合。

**运行环境快照（`Runtime Environment Snapshot`）**：
单个执行 turn 开始时生成、在该 turn 内保持不变的一版运行环境上下文。

## 网络能力

**网络搜索**：
根据查询条件从互联网中发现相关信息来源，主要解决“去哪里找”的问题。
_Avoid_: 网页抓取、浏览网页

**网页抓取**：
读取指定 URL 所指向的资源内容，主要解决“读取这个地址”的问题。
_Avoid_: 网络搜索、浏览器操作

**公共网络资源**：
可通过公共互联网路由访问，且访问过程不进入本机、局域网或其他非公开地址空间的资源。
_Avoid_: 外网资源

**私有网络资源**：
位于本机、局域网、链路本地地址或其他非公开地址空间中的资源；它与公共网络资源具有不同的信任边界。
_Avoid_: 内部网页

**私有网络访问开关（`web_fetch_private_network_enabled`）**：
决定当前应用是否具备访问私有网络资源的能力总闸；开启能力不等于授予具体访问权限。
_Avoid_: 内网授权

**网络来源（`Network Origin`）**：
由 URL 的协议、规范化主机名和有效端口共同标识的网络授权范围。

**系统硬拒绝网络目标**：
无论权限模式和私有网络访问开关如何配置都不能访问的目标，包括云平台元数据端点、未指定地址、广播地址和组播地址。
